<%= form_for @city do |city| %>
<!-- form_for is my best friend, does lots -->
  <div class="form-group">
    <%= form.label :city_name %>
    <%= form.text_field :city_name, class: "form-control" %>
  </div>
  <%= form.button 'Update City', class: "btn btn-success" %>
<% end %>